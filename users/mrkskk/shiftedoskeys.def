// clang-format off
// for custom shifted keypresses combining keys that differ between os.
// Will not work if the new shifted placemen off keys is already occupied by another defined key.
//for example. HAT is shifted DIAE so DIAE cannot be shifted into anything else

//        NAME,     shifted mac,        shifted win,        unshifted mac,       unshifted win
//SFT_OSKEY(S_LBRC,   LCB_MAC,             LCB_WIN,           LBRC,                   LBRC)
//SFT_OSKEY(S_RBRC,   RCB_MAC,             RCB_WIN,           RBRC,                   RBRC)
//SFT_OSKEY(S_EQL,    EXLM,                EXLM,              EQL,                    EQL)

//SFT_OSKEY(S_PIPE,   AMPR,                AMPR,              PIPE_MAC,               PIPE_WIN)

//SFT_OSKEY(S_PERC,   HAT,                 HAT,               PERC,                   PERC)       // ^ on Shifted %
SFT_OSKEY(S_QUOT,   DQUO,                DQUO,              QUOT,                   QUOT)       // " on Shifted '
//SFT_OSKEY(S_MINUS,  KC_PPLS,             KC_PPLS,           MINUS,                  MINUS)      // + on Shifted -


// should be changed to work with my mod instead...then any combination is possible
